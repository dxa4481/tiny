name: docs
on:
  push:
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # FIXED: Updated from @tt10 to @ttihp25a for IHP-SG13G2 PDK compatibility
      - name: Build docs
        uses: TinyTapeout/tt-gds-action/docs@ttihp25a
